/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.2.0
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import CreateBatchProductInsights422Response from '../model/CreateBatchProductInsights422Response';
import CreateBatchProductInsights500Response from '../model/CreateBatchProductInsights500Response';
import CreateProductInsights400Response from '../model/CreateProductInsights400Response';
import ErrorResponse from '../model/ErrorResponse';
import GetGroupedForecast404Response from '../model/GetGroupedForecast404Response';
import ProductInsightsBatchRequest from '../model/ProductInsightsBatchRequest';
import ProductInsightsBatchStatusResponse from '../model/ProductInsightsBatchStatusResponse';
import ProductInsightsRequest from '../model/ProductInsightsRequest';
import ProductInsightsResponse from '../model/ProductInsightsResponse';
import ValidationFailedResponse from '../model/ValidationFailedResponse';

/**
* ProductInsights service.
* @module api/ProductInsightsApi
* @version 2.2.0
*/
export default class ProductInsightsApi {

    /**
    * Constructs a new ProductInsightsApi. 
    * @alias module:api/ProductInsightsApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }



    /**
     * Submit a batch of products for insights analysis
     * Analyze up to 1,000 products in one asynchronous batch; each entry is the same shape as a single `/v2/product-insights` request. Returns `202` with the batch resource to poll via `GET /v2/batch/product-insights/{batch_id}`. The optional `batch` and `parts` fields let a large batch be submitted in chunks that share one batch id. Validation is per series: if any entry fails, the whole submission is rejected with the failing entries listed by index. 
     * @param {module:model/ProductInsightsBatchRequest} productInsightsBatchRequest 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/ProductInsightsBatchStatusResponse} and HTTP response
     */
    createBatchProductInsightsWithHttpInfo(productInsightsBatchRequest) {
      let postBody = productInsightsBatchRequest;
      // verify the required parameter 'productInsightsBatchRequest' is set
      if (productInsightsBatchRequest === undefined || productInsightsBatchRequest === null) {
        throw new Error("Missing the required parameter 'productInsightsBatchRequest' when calling createBatchProductInsights");
      }

      let pathParams = {
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['apiKey'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = ProductInsightsBatchStatusResponse;
      return this.apiClient.callApi(
        '/v2/batch/product-insights', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Submit a batch of products for insights analysis
     * Analyze up to 1,000 products in one asynchronous batch; each entry is the same shape as a single `/v2/product-insights` request. Returns `202` with the batch resource to poll via `GET /v2/batch/product-insights/{batch_id}`. The optional `batch` and `parts` fields let a large batch be submitted in chunks that share one batch id. Validation is per series: if any entry fails, the whole submission is rejected with the failing entries listed by index. 
     * @param {module:model/ProductInsightsBatchRequest} productInsightsBatchRequest 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/ProductInsightsBatchStatusResponse}
     */
    createBatchProductInsights(productInsightsBatchRequest) {
      return this.createBatchProductInsightsWithHttpInfo(productInsightsBatchRequest)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Analyze product performance for anomalies, trends and business insights
     * Analyzes a product's sales, purchase and cost-history data and returns classified insights — margin compression, sales anomalies, cost changes, lead-time reliability and more — each with severity, the metrics behind it and a recommendation. At least one of `sales`, `purchases` or `cost_history` must be provided. Analyses are stored per product, so repeat calls also return a `comparison` against the previous analysis with a new/changed/resolved breakdown. 
     * @param {module:model/ProductInsightsRequest} productInsightsRequest 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/ProductInsightsResponse} and HTTP response
     */
    createProductInsightsWithHttpInfo(productInsightsRequest) {
      let postBody = productInsightsRequest;
      // verify the required parameter 'productInsightsRequest' is set
      if (productInsightsRequest === undefined || productInsightsRequest === null) {
        throw new Error("Missing the required parameter 'productInsightsRequest' when calling createProductInsights");
      }

      let pathParams = {
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['apiKey'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = ProductInsightsResponse;
      return this.apiClient.callApi(
        '/v2/product-insights', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Analyze product performance for anomalies, trends and business insights
     * Analyzes a product's sales, purchase and cost-history data and returns classified insights — margin compression, sales anomalies, cost changes, lead-time reliability and more — each with severity, the metrics behind it and a recommendation. At least one of `sales`, `purchases` or `cost_history` must be provided. Analyses are stored per product, so repeat calls also return a `comparison` against the previous analysis with a new/changed/resolved breakdown. 
     * @param {module:model/ProductInsightsRequest} productInsightsRequest 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/ProductInsightsResponse}
     */
    createProductInsights(productInsightsRequest) {
      return this.createProductInsightsWithHttpInfo(productInsightsRequest)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Poll a product insights batch for status and results
     * @param {String} batchId The batch identifier returned on submission
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/ProductInsightsBatchStatusResponse} and HTTP response
     */
    getProductInsightsBatchWithHttpInfo(batchId) {
      let postBody = null;
      // verify the required parameter 'batchId' is set
      if (batchId === undefined || batchId === null) {
        throw new Error("Missing the required parameter 'batchId' when calling getProductInsightsBatch");
      }

      let pathParams = {
        'batch_id': batchId
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['apiKey'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ProductInsightsBatchStatusResponse;
      return this.apiClient.callApi(
        '/v2/batch/product-insights/{batch_id}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Poll a product insights batch for status and results
     * @param {String} batchId The batch identifier returned on submission
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/ProductInsightsBatchStatusResponse}
     */
    getProductInsightsBatch(batchId) {
      return this.getProductInsightsBatchWithHttpInfo(batchId)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


}
