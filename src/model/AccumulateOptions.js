/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.2.0
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AccumulateOptions model module.
 * @module model/AccumulateOptions
 * @version 2.2.0
 */
class AccumulateOptions {
    /**
     * Constructs a new <code>AccumulateOptions</code>.
     * Collapse the forecast path into a cumulative total — Σ (value × survival × discount). The same operation covers cumulative demand over a lead time (safety stock), customer lifetime value, TCO and NPV. Returns an &#x60;accumulated&#x60; block. Honoured by /v2/forecast and /v2/batch/forecast (request-level default or per-series override); rejected on grouped forecasts; ignored by other endpoints sharing this request shape. 
     * @alias module:model/AccumulateOptions
     */
    constructor() { 
        
        AccumulateOptions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['decay'] = 1.0;
        obj['discount_rate'] = 0;
        obj['correlation'] = 0.5;
    }

    /**
     * Constructs a <code>AccumulateOptions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccumulateOptions} obj Optional instance to populate.
     * @return {module:model/AccumulateOptions} The populated <code>AccumulateOptions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccumulateOptions();

            if (data.hasOwnProperty('decay')) {
                obj['decay'] = ApiClient.convertToType(data['decay'], 'Number');
            }
            if (data.hasOwnProperty('discount_rate')) {
                obj['discount_rate'] = ApiClient.convertToType(data['discount_rate'], 'Number');
            }
            if (data.hasOwnProperty('correlation')) {
                obj['correlation'] = ApiClient.convertToType(data['correlation'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccumulateOptions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccumulateOptions</code>.
     */
    static validateJSON(data) {

        return true;
    }


}



/**
 * Per-period survival factor — 0.95 means 5% attrition each period
 * @member {Number} decay
 * @default 1.0
 */
AccumulateOptions.prototype['decay'] = 1.0;

/**
 * Annual discount rate, converted to the series frequency (a 10% annual rate on monthly periods discounts period 2 by 1.10^(-1/12), not by 1.10)
 * @member {Number} discount_rate
 * @default 0
 */
AccumulateOptions.prototype['discount_rate'] = 0;

/**
 * How correlated the per-period errors are assumed to be; controls only the width of the total's band. 0 = independent periods (measurably too narrow), 1 = sum of the bounds (conservative envelope). The default 0.5 was measured across the benchmark suite; a value you supply is your assumption and is never labelled measured. 
 * @member {Number} correlation
 * @default 0.5
 */
AccumulateOptions.prototype['correlation'] = 0.5;






export default AccumulateOptions;

