/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.0.2
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GroupedForecastAccepted model module.
 * @module model/GroupedForecastAccepted
 * @version 2.0.2
 */
class GroupedForecastAccepted {
    /**
     * Constructs a new <code>GroupedForecastAccepted</code>.
     * @alias module:model/GroupedForecastAccepted
     */
    constructor() { 
        
        GroupedForecastAccepted.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GroupedForecastAccepted</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupedForecastAccepted} obj Optional instance to populate.
     * @return {module:model/GroupedForecastAccepted} The populated <code>GroupedForecastAccepted</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupedForecastAccepted();

            if (data.hasOwnProperty('grouped_forecast_id')) {
                obj['grouped_forecast_id'] = ApiClient.convertToType(data['grouped_forecast_id'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('series_count')) {
                obj['series_count'] = ApiClient.convertToType(data['series_count'], 'Number');
            }
            if (data.hasOwnProperty('node_count')) {
                obj['node_count'] = ApiClient.convertToType(data['node_count'], 'Number');
            }
            if (data.hasOwnProperty('reconciliation')) {
                obj['reconciliation'] = ApiClient.convertToType(data['reconciliation'], 'String');
            }
            if (data.hasOwnProperty('time_taken_ms')) {
                obj['time_taken_ms'] = ApiClient.convertToType(data['time_taken_ms'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupedForecastAccepted</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupedForecastAccepted</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['grouped_forecast_id'] && !(typeof data['grouped_forecast_id'] === 'string' || data['grouped_forecast_id'] instanceof String)) {
            throw new Error("Expected the field `grouped_forecast_id` to be a primitive type in the JSON string but got " + data['grouped_forecast_id']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['reconciliation'] && !(typeof data['reconciliation'] === 'string' || data['reconciliation'] instanceof String)) {
            throw new Error("Expected the field `reconciliation` to be a primitive type in the JSON string but got " + data['reconciliation']);
        }

        return true;
    }


}



/**
 * Id to poll via GET /v2/forecast/grouped/{grouped_forecast_id}
 * @member {String} grouped_forecast_id
 */
GroupedForecastAccepted.prototype['grouped_forecast_id'] = undefined;

/**
 * @member {String} status
 */
GroupedForecastAccepted.prototype['status'] = undefined;

/**
 * Leaf series submitted
 * @member {Number} series_count
 */
GroupedForecastAccepted.prototype['series_count'] = undefined;

/**
 * What will actually be forecast and reconciled — the leaves plus every derived aggregate and the total
 * @member {Number} node_count
 */
GroupedForecastAccepted.prototype['node_count'] = undefined;

/**
 * @member {module:model/GroupedForecastAccepted.ReconciliationEnum} reconciliation
 */
GroupedForecastAccepted.prototype['reconciliation'] = undefined;

/**
 * @member {Number} time_taken_ms
 */
GroupedForecastAccepted.prototype['time_taken_ms'] = undefined;





/**
 * Allowed values for the <code>reconciliation</code> property.
 * @enum {String}
 * @readonly
 */
GroupedForecastAccepted['ReconciliationEnum'] = {

    /**
     * value: "bottom_up"
     * @const
     */
    "bottom_up": "bottom_up",

    /**
     * value: "min_trace"
     * @const
     */
    "min_trace": "min_trace"
};



export default GroupedForecastAccepted;

