/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.0.2
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TrafficForecastingResponseResultCapacityAnalysisUtilizationPeriodsInner from './TrafficForecastingResponseResultCapacityAnalysisUtilizationPeriodsInner';

/**
 * The TrafficForecastingResponseResultCapacityAnalysis model module.
 * @module model/TrafficForecastingResponseResultCapacityAnalysis
 * @version 2.0.2
 */
class TrafficForecastingResponseResultCapacityAnalysis {
    /**
     * Constructs a new <code>TrafficForecastingResponseResultCapacityAnalysis</code>.
     * Detailed capacity utilization analysis
     * @alias module:model/TrafficForecastingResponseResultCapacityAnalysis
     */
    constructor() { 
        
        TrafficForecastingResponseResultCapacityAnalysis.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TrafficForecastingResponseResultCapacityAnalysis</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrafficForecastingResponseResultCapacityAnalysis} obj Optional instance to populate.
     * @return {module:model/TrafficForecastingResponseResultCapacityAnalysis} The populated <code>TrafficForecastingResponseResultCapacityAnalysis</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrafficForecastingResponseResultCapacityAnalysis();

            if (data.hasOwnProperty('current_capacity')) {
                obj['current_capacity'] = ApiClient.convertToType(data['current_capacity'], 'Number');
            }
            if (data.hasOwnProperty('utilization_periods')) {
                obj['utilization_periods'] = ApiClient.convertToType(data['utilization_periods'], [TrafficForecastingResponseResultCapacityAnalysisUtilizationPeriodsInner]);
            }
            if (data.hasOwnProperty('over_capacity_periods')) {
                obj['over_capacity_periods'] = ApiClient.convertToType(data['over_capacity_periods'], 'Number');
            }
            if (data.hasOwnProperty('critical_periods')) {
                obj['critical_periods'] = ApiClient.convertToType(data['critical_periods'], 'Number');
            }
            if (data.hasOwnProperty('forecast_duration_minutes')) {
                obj['forecast_duration_minutes'] = ApiClient.convertToType(data['forecast_duration_minutes'], 'Number');
            }
            if (data.hasOwnProperty('next_capacity_breach')) {
                obj['next_capacity_breach'] = ApiClient.convertToType(data['next_capacity_breach'], 'Date');
            }
            if (data.hasOwnProperty('capacity_efficiency')) {
                obj['capacity_efficiency'] = ApiClient.convertToType(data['capacity_efficiency'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrafficForecastingResponseResultCapacityAnalysis</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrafficForecastingResponseResultCapacityAnalysis</code>.
     */
    static validateJSON(data) {
        if (data['utilization_periods']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['utilization_periods'])) {
                throw new Error("Expected the field `utilization_periods` to be an array in the JSON data but got " + data['utilization_periods']);
            }
            // validate the optional field `utilization_periods` (array)
            for (const item of data['utilization_periods']) {
                TrafficForecastingResponseResultCapacityAnalysisUtilizationPeriodsInner.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Current infrastructure capacity
 * @member {Number} current_capacity
 */
TrafficForecastingResponseResultCapacityAnalysis.prototype['current_capacity'] = undefined;

/**
 * Capacity utilization for each forecast period
 * @member {Array.<module:model/TrafficForecastingResponseResultCapacityAnalysisUtilizationPeriodsInner>} utilization_periods
 */
TrafficForecastingResponseResultCapacityAnalysis.prototype['utilization_periods'] = undefined;

/**
 * Number of periods exceeding capacity
 * @member {Number} over_capacity_periods
 */
TrafficForecastingResponseResultCapacityAnalysis.prototype['over_capacity_periods'] = undefined;

/**
 * Number of periods above 90% utilization
 * @member {Number} critical_periods
 */
TrafficForecastingResponseResultCapacityAnalysis.prototype['critical_periods'] = undefined;

/**
 * Total forecast duration in minutes
 * @member {Number} forecast_duration_minutes
 */
TrafficForecastingResponseResultCapacityAnalysis.prototype['forecast_duration_minutes'] = undefined;

/**
 * Date when capacity will be breached
 * @member {Date} next_capacity_breach
 */
TrafficForecastingResponseResultCapacityAnalysis.prototype['next_capacity_breach'] = undefined;

/**
 * Efficiency score (0-100, optimal around 75% utilization)
 * @member {Number} capacity_efficiency
 */
TrafficForecastingResponseResultCapacityAnalysis.prototype['capacity_efficiency'] = undefined;






export default TrafficForecastingResponseResultCapacityAnalysis;

