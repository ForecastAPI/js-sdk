/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GroupedForecastSeries from './GroupedForecastSeries';

/**
 * The GroupedForecastRequest model module.
 * @module model/GroupedForecastRequest
 * @version 2.0.0
 */
class GroupedForecastRequest {
    /**
     * Constructs a new <code>GroupedForecastRequest</code>.
     * Configuration is request-level only: every node of a hierarchy must share &#x60;periods&#x60;, &#x60;frequency&#x60;, &#x60;model&#x60; and &#x60;confidence&#x60;, so per-series overrides are rejected rather than silently ignored. The planning parameters (&#x60;quantiles&#x60;, &#x60;value_bounds&#x60;, &#x60;adjustments&#x60;, &#x60;accumulate&#x60;) are not supported on grouped forecasts and are rejected explicitly. All series must end on the same date so forecast periods align; histories may start whenever they like — a segment that launched later simply contributes zero before its first data point. A request may expand to at most 250 nodes (leaves plus every derived aggregate and the total) across at most 4 hierarchy levels. 
     * @alias module:model/GroupedForecastRequest
     * @param identifier {String} Names the measure being forecast (e.g. \"mrr\"). The hierarchy's total is stored under this identifier exactly like a plain /v2/forecast for it — the total of the hierarchy IS the measure. 
     * @param hierarchy {Array.<String>} The dimensions of the hierarchy, outermost first. Each series' segment must name exactly these dimensions.
     * @param frequency {module:model/GroupedForecastRequest.FrequencyEnum} Data frequency, shared by every series in the hierarchy
     * @param periods {Number} Number of periods to forecast, shared by every node
     * @param series {Array.<module:model/GroupedForecastSeries>} The leaves of the hierarchy — one entry per combination of dimension values, each combination exactly once. Up to 200 series on a paid plan, 10 on the free tier. Aggregate levels are derived by the API and must not be sent. 
     */
    constructor(identifier, hierarchy, frequency, periods, series) { 
        
        GroupedForecastRequest.initialize(this, identifier, hierarchy, frequency, periods, series);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, identifier, hierarchy, frequency, periods, series) { 
        obj['identifier'] = identifier;
        obj['hierarchy'] = hierarchy;
        obj['reconciliation'] = 'bottom_up';
        obj['correlation'] = 0.5;
        obj['frequency'] = frequency;
        obj['periods'] = periods;
        obj['data_type'] = 'sales';
        obj['model'] = 'standard';
        obj['confidence'] = 0.8;
        obj['series'] = series;
    }

    /**
     * Constructs a <code>GroupedForecastRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupedForecastRequest} obj Optional instance to populate.
     * @return {module:model/GroupedForecastRequest} The populated <code>GroupedForecastRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupedForecastRequest();

            if (data.hasOwnProperty('identifier')) {
                obj['identifier'] = ApiClient.convertToType(data['identifier'], 'String');
            }
            if (data.hasOwnProperty('tenant_context')) {
                obj['tenant_context'] = ApiClient.convertToType(data['tenant_context'], 'String');
            }
            if (data.hasOwnProperty('hierarchy')) {
                obj['hierarchy'] = ApiClient.convertToType(data['hierarchy'], ['String']);
            }
            if (data.hasOwnProperty('reconciliation')) {
                obj['reconciliation'] = ApiClient.convertToType(data['reconciliation'], 'String');
            }
            if (data.hasOwnProperty('correlation')) {
                obj['correlation'] = ApiClient.convertToType(data['correlation'], 'Number');
            }
            if (data.hasOwnProperty('frequency')) {
                obj['frequency'] = ApiClient.convertToType(data['frequency'], 'String');
            }
            if (data.hasOwnProperty('periods')) {
                obj['periods'] = ApiClient.convertToType(data['periods'], 'Number');
            }
            if (data.hasOwnProperty('data_type')) {
                obj['data_type'] = ApiClient.convertToType(data['data_type'], 'String');
            }
            if (data.hasOwnProperty('model')) {
                obj['model'] = ApiClient.convertToType(data['model'], 'String');
            }
            if (data.hasOwnProperty('confidence')) {
                obj['confidence'] = ApiClient.convertToType(data['confidence'], 'Number');
            }
            if (data.hasOwnProperty('confidence_level')) {
                obj['confidence_level'] = ApiClient.convertToType(data['confidence_level'], 'Number');
            }
            if (data.hasOwnProperty('series')) {
                obj['series'] = ApiClient.convertToType(data['series'], [GroupedForecastSeries]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupedForecastRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupedForecastRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GroupedForecastRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['identifier'] && !(typeof data['identifier'] === 'string' || data['identifier'] instanceof String)) {
            throw new Error("Expected the field `identifier` to be a primitive type in the JSON string but got " + data['identifier']);
        }
        // ensure the json data is a string
        if (data['tenant_context'] && !(typeof data['tenant_context'] === 'string' || data['tenant_context'] instanceof String)) {
            throw new Error("Expected the field `tenant_context` to be a primitive type in the JSON string but got " + data['tenant_context']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['hierarchy'])) {
            throw new Error("Expected the field `hierarchy` to be an array in the JSON data but got " + data['hierarchy']);
        }
        // ensure the json data is a string
        if (data['reconciliation'] && !(typeof data['reconciliation'] === 'string' || data['reconciliation'] instanceof String)) {
            throw new Error("Expected the field `reconciliation` to be a primitive type in the JSON string but got " + data['reconciliation']);
        }
        // ensure the json data is a string
        if (data['frequency'] && !(typeof data['frequency'] === 'string' || data['frequency'] instanceof String)) {
            throw new Error("Expected the field `frequency` to be a primitive type in the JSON string but got " + data['frequency']);
        }
        // ensure the json data is a string
        if (data['data_type'] && !(typeof data['data_type'] === 'string' || data['data_type'] instanceof String)) {
            throw new Error("Expected the field `data_type` to be a primitive type in the JSON string but got " + data['data_type']);
        }
        // ensure the json data is a string
        if (data['model'] && !(typeof data['model'] === 'string' || data['model'] instanceof String)) {
            throw new Error("Expected the field `model` to be a primitive type in the JSON string but got " + data['model']);
        }
        if (data['series']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['series'])) {
                throw new Error("Expected the field `series` to be an array in the JSON data but got " + data['series']);
            }
            // validate the optional field `series` (array)
            for (const item of data['series']) {
                GroupedForecastSeries.validateJSON(item);
            };
        }

        return true;
    }


}

GroupedForecastRequest.RequiredProperties = ["identifier", "hierarchy", "frequency", "periods", "series"];

/**
 * Names the measure being forecast (e.g. \"mrr\"). The hierarchy's total is stored under this identifier exactly like a plain /v2/forecast for it — the total of the hierarchy IS the measure. 
 * @member {String} identifier
 */
GroupedForecastRequest.prototype['identifier'] = undefined;

/**
 * Optional tenant scope, independent of segment — tenancy keeps meaning \"whose data is this\", segment means \"which slice\". 
 * @member {String} tenant_context
 */
GroupedForecastRequest.prototype['tenant_context'] = undefined;

/**
 * The dimensions of the hierarchy, outermost first. Each series' segment must name exactly these dimensions.
 * @member {Array.<String>} hierarchy
 */
GroupedForecastRequest.prototype['hierarchy'] = undefined;

/**
 * - bottom_up (default): only the leaves are forecast; every aggregate is the   exact sum of its children. Fast and cheap; best when leaf histories are   substantial enough to forecast well on their own. - min_trace: every node — aggregates included — is forecast, then MinTrace   optimally redistributes the disagreement between levels into a coherent set.   Costs one forecast per node, but lets the usually-smoother aggregate series   inform the result, which tends to help when leaves are short or noisy.  top_down is not supported and is rejected by name. 
 * @member {module:model/GroupedForecastRequest.ReconciliationEnum} reconciliation
 * @default 'bottom_up'
 */
GroupedForecastRequest.prototype['reconciliation'] = 'bottom_up';

/**
 * bottom_up only — the assumed correlation between sibling errors when building aggregate confidence bands. 0 = independent siblings, 1 = perfectly correlated (equivalent to summing the bounds). Echoed back in the response and never labelled as measured. 
 * @member {Number} correlation
 * @default 0.5
 */
GroupedForecastRequest.prototype['correlation'] = 0.5;

/**
 * Data frequency, shared by every series in the hierarchy
 * @member {module:model/GroupedForecastRequest.FrequencyEnum} frequency
 */
GroupedForecastRequest.prototype['frequency'] = undefined;

/**
 * Number of periods to forecast, shared by every node
 * @member {Number} periods
 */
GroupedForecastRequest.prototype['periods'] = undefined;

/**
 * Type of data (e.g., sales, demand, revenue). Sales-family types get non-negative reconciliation treatment.
 * @member {String} data_type
 * @default 'sales'
 */
GroupedForecastRequest.prototype['data_type'] = 'sales';

/**
 * Forecasting engine used for every node
 * @member {module:model/GroupedForecastRequest.ModelEnum} model
 * @default 'standard'
 */
GroupedForecastRequest.prototype['model'] = 'standard';

/**
 * Confidence level for prediction intervals, shared by every node
 * @member {Number} confidence
 * @default 0.8
 */
GroupedForecastRequest.prototype['confidence'] = 0.8;

/**
 * Alias for `confidence`; if both are sent, `confidence` wins
 * @member {Number} confidence_level
 */
GroupedForecastRequest.prototype['confidence_level'] = undefined;

/**
 * The leaves of the hierarchy — one entry per combination of dimension values, each combination exactly once. Up to 200 series on a paid plan, 10 on the free tier. Aggregate levels are derived by the API and must not be sent. 
 * @member {Array.<module:model/GroupedForecastSeries>} series
 */
GroupedForecastRequest.prototype['series'] = undefined;





/**
 * Allowed values for the <code>reconciliation</code> property.
 * @enum {String}
 * @readonly
 */
GroupedForecastRequest['ReconciliationEnum'] = {

    /**
     * value: "bottom_up"
     * @const
     */
    "bottom_up": "bottom_up",

    /**
     * value: "min_trace"
     * @const
     */
    "min_trace": "min_trace"
};


/**
 * Allowed values for the <code>frequency</code> property.
 * @enum {String}
 * @readonly
 */
GroupedForecastRequest['FrequencyEnum'] = {

    /**
     * value: "H"
     * @const
     */
    "H": "H",

    /**
     * value: "D"
     * @const
     */
    "D": "D",

    /**
     * value: "W"
     * @const
     */
    "W": "W",

    /**
     * value: "M"
     * @const
     */
    "M": "M",

    /**
     * value: "MS"
     * @const
     */
    "MS": "MS",

    /**
     * value: "ME"
     * @const
     */
    "ME": "ME",

    /**
     * value: "Q"
     * @const
     */
    "Q": "Q",

    /**
     * value: "Y"
     * @const
     */
    "Y": "Y"
};


/**
 * Allowed values for the <code>model</code> property.
 * @enum {String}
 * @readonly
 */
GroupedForecastRequest['ModelEnum'] = {

    /**
     * value: "standard"
     * @const
     */
    "standard": "standard",

    /**
     * value: "advanced"
     * @const
     */
    "advanced": "advanced",

    /**
     * value: "advanced-quantized"
     * @const
     */
    "advanced-quantized": "advanced-quantized",

    /**
     * value: "advanced-patched"
     * @const
     */
    "advanced-patched": "advanced-patched",

    /**
     * value: "ensemble"
     * @const
     */
    "ensemble": "ensemble"
};



export default GroupedForecastRequest;

