/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AccumulatedForecastAssumptions from './AccumulatedForecastAssumptions';
import AccumulatedForecastByPeriodInner from './AccumulatedForecastByPeriodInner';

/**
 * The AccumulatedForecast model module.
 * @module model/AccumulatedForecast
 * @version 2.0.0
 */
class AccumulatedForecast {
    /**
     * Constructs a new <code>AccumulatedForecast</code>.
     * Cumulative projection over the forecast path. Present only when &#x60;accumulate&#x60; was sent, as a sibling of &#x60;result&#x60;. 
     * @alias module:model/AccumulatedForecast
     */
    constructor() { 
        
        AccumulatedForecast.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AccumulatedForecast</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccumulatedForecast} obj Optional instance to populate.
     * @return {module:model/AccumulatedForecast} The populated <code>AccumulatedForecast</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccumulatedForecast();

            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('total_reason')) {
                obj['total_reason'] = ApiClient.convertToType(data['total_reason'], 'String');
            }
            if (data.hasOwnProperty('lower')) {
                obj['lower'] = ApiClient.convertToType(data['lower'], 'Number');
            }
            if (data.hasOwnProperty('upper')) {
                obj['upper'] = ApiClient.convertToType(data['upper'], 'Number');
            }
            if (data.hasOwnProperty('interval_reason')) {
                obj['interval_reason'] = ApiClient.convertToType(data['interval_reason'], 'String');
            }
            if (data.hasOwnProperty('confidence_level')) {
                obj['confidence_level'] = ApiClient.convertToType(data['confidence_level'], 'Number');
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('assumptions')) {
                obj['assumptions'] = AccumulatedForecastAssumptions.constructFromObject(data['assumptions']);
            }
            if (data.hasOwnProperty('by_period')) {
                obj['by_period'] = ApiClient.convertToType(data['by_period'], [AccumulatedForecastByPeriodInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccumulatedForecast</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccumulatedForecast</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['total_reason'] && !(typeof data['total_reason'] === 'string' || data['total_reason'] instanceof String)) {
            throw new Error("Expected the field `total_reason` to be a primitive type in the JSON string but got " + data['total_reason']);
        }
        // ensure the json data is a string
        if (data['interval_reason'] && !(typeof data['interval_reason'] === 'string' || data['interval_reason'] instanceof String)) {
            throw new Error("Expected the field `interval_reason` to be a primitive type in the JSON string but got " + data['interval_reason']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }
        // validate the optional field `assumptions`
        if (data['assumptions']) { // data not null
          AccumulatedForecastAssumptions.validateJSON(data['assumptions']);
        }
        if (data['by_period']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['by_period'])) {
                throw new Error("Expected the field `by_period` to be an array in the JSON data but got " + data['by_period']);
            }
            // validate the optional field `by_period` (array)
            for (const item of data['by_period']) {
                AccumulatedForecastByPeriodInner.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Σ (value × survival × discount) over the horizon. Null when a period lacks a usable point forecast (see `total_reason`).
 * @member {Number} total
 */
AccumulatedForecast.prototype['total'] = undefined;

/**
 * Why `total` is null; null when the total is usable
 * @member {String} total_reason
 */
AccumulatedForecast.prototype['total_reason'] = undefined;

/**
 * Lower bound of the total under the stated correlation assumption. Null with an `interval_reason` when any period lacks a band or its coverage is unknown — rather than a band built from a partial set of variances. 
 * @member {Number} lower
 */
AccumulatedForecast.prototype['lower'] = undefined;

/**
 * @member {Number} upper
 */
AccumulatedForecast.prototype['upper'] = undefined;

/**
 * Why the total's band is absent; null when the band is present
 * @member {String} interval_reason
 */
AccumulatedForecast.prototype['interval_reason'] = undefined;

/**
 * Coverage the total's band targets
 * @member {Number} confidence_level
 */
AccumulatedForecast.prototype['confidence_level'] = undefined;

/**
 * Which path was accumulated — `adjusted` when `adjustments` was also sent, otherwise `baseline`
 * @member {module:model/AccumulatedForecast.PathEnum} path
 */
AccumulatedForecast.prototype['path'] = undefined;

/**
 * @member {module:model/AccumulatedForecastAssumptions} assumptions
 */
AccumulatedForecast.prototype['assumptions'] = undefined;

/**
 * Running accumulation per period — `cumulative` through period L is the number a lead-time/safety-stock calculation reads
 * @member {Array.<module:model/AccumulatedForecastByPeriodInner>} by_period
 */
AccumulatedForecast.prototype['by_period'] = undefined;





/**
 * Allowed values for the <code>path</code> property.
 * @enum {String}
 * @readonly
 */
AccumulatedForecast['PathEnum'] = {

    /**
     * value: "baseline"
     * @const
     */
    "baseline": "baseline",

    /**
     * value: "adjusted"
     * @const
     */
    "adjusted": "adjusted"
};



export default AccumulatedForecast;

