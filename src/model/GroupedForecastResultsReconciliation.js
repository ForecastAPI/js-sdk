/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.2.0
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GroupedForecastResultsReconciliation model module.
 * @module model/GroupedForecastResultsReconciliation
 * @version 2.2.0
 */
class GroupedForecastResultsReconciliation {
    /**
     * Constructs a new <code>GroupedForecastResultsReconciliation</code>.
     * How coherence was achieved. bottom_up reports the sibling correlation in force, its source, and &#x60;measured: false&#x60; — the correlation is a stated assumption, not a fitted number. min_trace reports the MinTrace variant and whether the non-negative solve was applied. 
     * @alias module:model/GroupedForecastResultsReconciliation
     */
    constructor() { 
        
        GroupedForecastResultsReconciliation.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GroupedForecastResultsReconciliation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupedForecastResultsReconciliation} obj Optional instance to populate.
     * @return {module:model/GroupedForecastResultsReconciliation} The populated <code>GroupedForecastResultsReconciliation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupedForecastResultsReconciliation();

            if (data.hasOwnProperty('method')) {
                obj['method'] = ApiClient.convertToType(data['method'], 'String');
            }
            if (data.hasOwnProperty('correlation')) {
                obj['correlation'] = ApiClient.convertToType(data['correlation'], 'Number');
            }
            if (data.hasOwnProperty('correlation_source')) {
                obj['correlation_source'] = ApiClient.convertToType(data['correlation_source'], 'String');
            }
            if (data.hasOwnProperty('measured')) {
                obj['measured'] = ApiClient.convertToType(data['measured'], 'Boolean');
            }
            if (data.hasOwnProperty('variant')) {
                obj['variant'] = ApiClient.convertToType(data['variant'], 'String');
            }
            if (data.hasOwnProperty('nonnegative')) {
                obj['nonnegative'] = ApiClient.convertToType(data['nonnegative'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupedForecastResultsReconciliation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupedForecastResultsReconciliation</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['method'] && !(typeof data['method'] === 'string' || data['method'] instanceof String)) {
            throw new Error("Expected the field `method` to be a primitive type in the JSON string but got " + data['method']);
        }
        // ensure the json data is a string
        if (data['correlation_source'] && !(typeof data['correlation_source'] === 'string' || data['correlation_source'] instanceof String)) {
            throw new Error("Expected the field `correlation_source` to be a primitive type in the JSON string but got " + data['correlation_source']);
        }
        // ensure the json data is a string
        if (data['variant'] && !(typeof data['variant'] === 'string' || data['variant'] instanceof String)) {
            throw new Error("Expected the field `variant` to be a primitive type in the JSON string but got " + data['variant']);
        }

        return true;
    }


}



/**
 * @member {module:model/GroupedForecastResultsReconciliation.MethodEnum} method
 */
GroupedForecastResultsReconciliation.prototype['method'] = undefined;

/**
 * bottom_up only
 * @member {Number} correlation
 */
GroupedForecastResultsReconciliation.prototype['correlation'] = undefined;

/**
 * bottom_up only
 * @member {module:model/GroupedForecastResultsReconciliation.CorrelationSourceEnum} correlation_source
 */
GroupedForecastResultsReconciliation.prototype['correlation_source'] = undefined;

/**
 * bottom_up only — always false; the cross-sibling correlation is an assumption
 * @member {Boolean} measured
 */
GroupedForecastResultsReconciliation.prototype['measured'] = undefined;

/**
 * min_trace only — the MinTrace weighting used
 * @member {String} variant
 */
GroupedForecastResultsReconciliation.prototype['variant'] = undefined;

/**
 * min_trace only — whether the non-negative solve was applied
 * @member {Boolean} nonnegative
 */
GroupedForecastResultsReconciliation.prototype['nonnegative'] = undefined;





/**
 * Allowed values for the <code>method</code> property.
 * @enum {String}
 * @readonly
 */
GroupedForecastResultsReconciliation['MethodEnum'] = {

    /**
     * value: "bottom_up"
     * @const
     */
    "bottom_up": "bottom_up",

    /**
     * value: "min_trace"
     * @const
     */
    "min_trace": "min_trace"
};


/**
 * Allowed values for the <code>correlation_source</code> property.
 * @enum {String}
 * @readonly
 */
GroupedForecastResultsReconciliation['CorrelationSourceEnum'] = {

    /**
     * value: "default"
     * @const
     */
    "default": "default",

    /**
     * value: "request"
     * @const
     */
    "request": "request"
};



export default GroupedForecastResultsReconciliation;

