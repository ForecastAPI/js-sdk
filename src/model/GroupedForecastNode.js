/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.0.2
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ForecastPeriod from './ForecastPeriod';

/**
 * The GroupedForecastNode model module.
 * @module model/GroupedForecastNode
 * @version 2.0.2
 */
class GroupedForecastNode {
    /**
     * Constructs a new <code>GroupedForecastNode</code>.
     * @alias module:model/GroupedForecastNode
     */
    constructor() { 
        
        GroupedForecastNode.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GroupedForecastNode</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupedForecastNode} obj Optional instance to populate.
     * @return {module:model/GroupedForecastNode} The populated <code>GroupedForecastNode</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupedForecastNode();

            if (data.hasOwnProperty('identifier')) {
                obj['identifier'] = ApiClient.convertToType(data['identifier'], 'String');
            }
            if (data.hasOwnProperty('segment')) {
                obj['segment'] = ApiClient.convertToType(data['segment'], {'String': Object});
            }
            if (data.hasOwnProperty('level')) {
                obj['level'] = ApiClient.convertToType(data['level'], 'Number');
            }
            if (data.hasOwnProperty('is_leaf')) {
                obj['is_leaf'] = ApiClient.convertToType(data['is_leaf'], 'Boolean');
            }
            if (data.hasOwnProperty('parent')) {
                obj['parent'] = ApiClient.convertToType(data['parent'], 'String');
            }
            if (data.hasOwnProperty('children')) {
                obj['children'] = ApiClient.convertToType(data['children'], ['String']);
            }
            if (data.hasOwnProperty('forecasts')) {
                obj['forecasts'] = ApiClient.convertToType(data['forecasts'], [ForecastPeriod]);
            }
            if (data.hasOwnProperty('model_info')) {
                obj['model_info'] = ApiClient.convertToType(data['model_info'], {'String': Object});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupedForecastNode</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupedForecastNode</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['identifier'] && !(typeof data['identifier'] === 'string' || data['identifier'] instanceof String)) {
            throw new Error("Expected the field `identifier` to be a primitive type in the JSON string but got " + data['identifier']);
        }
        // ensure the json data is a string
        if (data['parent'] && !(typeof data['parent'] === 'string' || data['parent'] instanceof String)) {
            throw new Error("Expected the field `parent` to be a primitive type in the JSON string but got " + data['parent']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['children'])) {
            throw new Error("Expected the field `children` to be an array in the JSON data but got " + data['children']);
        }
        if (data['forecasts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['forecasts'])) {
                throw new Error("Expected the field `forecasts` to be an array in the JSON data but got " + data['forecasts']);
            }
            // validate the optional field `forecasts` (array)
            for (const item of data['forecasts']) {
                ForecastPeriod.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The measure — the same for every node
 * @member {String} identifier
 */
GroupedForecastNode.prototype['identifier'] = undefined;

/**
 * The dimension values this node covers — empty for the total, all declared dimensions for a leaf
 * @member {Object.<String, Object>} segment
 */
GroupedForecastNode.prototype['segment'] = undefined;

/**
 * Depth in the tree; 0 is the total
 * @member {Number} level
 */
GroupedForecastNode.prototype['level'] = undefined;

/**
 * @member {Boolean} is_leaf
 */
GroupedForecastNode.prototype['is_leaf'] = undefined;

/**
 * Segment key of the parent node; null for the total
 * @member {String} parent
 */
GroupedForecastNode.prototype['parent'] = undefined;

/**
 * Segment keys of the children; empty for leaves
 * @member {Array.<String>} children
 */
GroupedForecastNode.prototype['children'] = undefined;

/**
 * The reconciled path — this is what is stored and accuracy-tracked per node
 * @member {Array.<module:model/ForecastPeriod>} forecasts
 */
GroupedForecastNode.prototype['forecasts'] = undefined;

/**
 * Method and reconciliation details for this node. With min_trace, the node's unreconciled path is reported under `reconciliation.base_forecast`. When an aggregate band cannot be built honestly (a child's band coverage is unknown), the band is omitted and `reconciliation.interval_reason` states why. 
 * @member {Object.<String, Object>} model_info
 */
GroupedForecastNode.prototype['model_info'] = undefined;






export default GroupedForecastNode;

