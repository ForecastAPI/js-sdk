/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.1.0
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AccumulatedForecastAssumptions model module.
 * @module model/AccumulatedForecastAssumptions
 * @version 2.1.0
 */
class AccumulatedForecastAssumptions {
    /**
     * Constructs a new <code>AccumulatedForecastAssumptions</code>.
     * The assumptions behind the total and its band, stated explicitly
     * @alias module:model/AccumulatedForecastAssumptions
     */
    constructor() { 
        
        AccumulatedForecastAssumptions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AccumulatedForecastAssumptions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccumulatedForecastAssumptions} obj Optional instance to populate.
     * @return {module:model/AccumulatedForecastAssumptions} The populated <code>AccumulatedForecastAssumptions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccumulatedForecastAssumptions();

            if (data.hasOwnProperty('correlation')) {
                obj['correlation'] = ApiClient.convertToType(data['correlation'], 'Number');
            }
            if (data.hasOwnProperty('correlation_source')) {
                obj['correlation_source'] = ApiClient.convertToType(data['correlation_source'], 'String');
            }
            if (data.hasOwnProperty('measured')) {
                obj['measured'] = ApiClient.convertToType(data['measured'], 'Boolean');
            }
            if (data.hasOwnProperty('basis')) {
                obj['basis'] = ApiClient.convertToType(data['basis'], 'String');
            }
            if (data.hasOwnProperty('decay')) {
                obj['decay'] = ApiClient.convertToType(data['decay'], 'Number');
            }
            if (data.hasOwnProperty('discount_rate')) {
                obj['discount_rate'] = ApiClient.convertToType(data['discount_rate'], 'Number');
            }
            if (data.hasOwnProperty('periods_per_year')) {
                obj['periods_per_year'] = ApiClient.convertToType(data['periods_per_year'], 'Number');
            }
            if (data.hasOwnProperty('timing')) {
                obj['timing'] = ApiClient.convertToType(data['timing'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccumulatedForecastAssumptions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccumulatedForecastAssumptions</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['correlation_source'] && !(typeof data['correlation_source'] === 'string' || data['correlation_source'] instanceof String)) {
            throw new Error("Expected the field `correlation_source` to be a primitive type in the JSON string but got " + data['correlation_source']);
        }
        // ensure the json data is a string
        if (data['basis'] && !(typeof data['basis'] === 'string' || data['basis'] instanceof String)) {
            throw new Error("Expected the field `basis` to be a primitive type in the JSON string but got " + data['basis']);
        }
        // ensure the json data is a string
        if (data['timing'] && !(typeof data['timing'] === 'string' || data['timing'] instanceof String)) {
            throw new Error("Expected the field `timing` to be a primitive type in the JSON string but got " + data['timing']);
        }

        return true;
    }


}



/**
 * @member {Number} correlation
 */
AccumulatedForecastAssumptions.prototype['correlation'] = undefined;

/**
 * @member {module:model/AccumulatedForecastAssumptions.CorrelationSourceEnum} correlation_source
 */
AccumulatedForecastAssumptions.prototype['correlation_source'] = undefined;

/**
 * True only when the correlation in force is our measured default — a caller-supplied value is never labelled measured
 * @member {Boolean} measured
 */
AccumulatedForecastAssumptions.prototype['measured'] = undefined;

/**
 * The statistical model behind the total's band
 * @member {String} basis
 */
AccumulatedForecastAssumptions.prototype['basis'] = undefined;

/**
 * @member {Number} decay
 */
AccumulatedForecastAssumptions.prototype['decay'] = undefined;

/**
 * @member {Number} discount_rate
 */
AccumulatedForecastAssumptions.prototype['discount_rate'] = undefined;

/**
 * How the annual discount rate was converted to the series frequency
 * @member {Number} periods_per_year
 */
AccumulatedForecastAssumptions.prototype['periods_per_year'] = undefined;

/**
 * @member {String} timing
 */
AccumulatedForecastAssumptions.prototype['timing'] = undefined;





/**
 * Allowed values for the <code>correlation_source</code> property.
 * @enum {String}
 * @readonly
 */
AccumulatedForecastAssumptions['CorrelationSourceEnum'] = {

    /**
     * value: "default"
     * @const
     */
    "default": "default",

    /**
     * value: "request"
     * @const
     */
    "request": "request"
};



export default AccumulatedForecastAssumptions;

