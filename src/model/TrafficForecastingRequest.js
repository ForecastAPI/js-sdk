/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.0.2
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TrafficForecastingRequestDataInner from './TrafficForecastingRequestDataInner';
import TrafficForecastingRequestTrafficSettings from './TrafficForecastingRequestTrafficSettings';

/**
 * The TrafficForecastingRequest model module.
 * @module model/TrafficForecastingRequest
 * @version 2.0.2
 */
class TrafficForecastingRequest {
    /**
     * Constructs a new <code>TrafficForecastingRequest</code>.
     * @alias module:model/TrafficForecastingRequest
     * @param identifier {String} Metric identifier (e.g., endpoint name, service name)
     * @param frequency {module:model/TrafficForecastingRequest.FrequencyEnum} Data frequency for forecasting: - M: Minute (for real-time traffic) - H: Hourly - D: Daily - W: Weekly - MS: Monthly (start of month) - ME: Monthly (end of month) - Q: Quarterly - Y: Yearly 
     * @param periods {Number} Number of periods to forecast ahead
     * @param data {Array.<module:model/TrafficForecastingRequestDataInner>} Historical traffic data for forecasting
     * @param trafficSettings {module:model/TrafficForecastingRequestTrafficSettings} 
     */
    constructor(identifier, frequency, periods, data, trafficSettings) { 
        
        TrafficForecastingRequest.initialize(this, identifier, frequency, periods, data, trafficSettings);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, identifier, frequency, periods, data, trafficSettings) { 
        obj['identifier'] = identifier;
        obj['frequency'] = frequency;
        obj['periods'] = periods;
        obj['data'] = data;
        obj['traffic_settings'] = trafficSettings;
    }

    /**
     * Constructs a <code>TrafficForecastingRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrafficForecastingRequest} obj Optional instance to populate.
     * @return {module:model/TrafficForecastingRequest} The populated <code>TrafficForecastingRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrafficForecastingRequest();

            if (data.hasOwnProperty('identifier')) {
                obj['identifier'] = ApiClient.convertToType(data['identifier'], 'String');
            }
            if (data.hasOwnProperty('frequency')) {
                obj['frequency'] = ApiClient.convertToType(data['frequency'], 'String');
            }
            if (data.hasOwnProperty('start_date')) {
                obj['start_date'] = ApiClient.convertToType(data['start_date'], 'Date');
            }
            if (data.hasOwnProperty('periods')) {
                obj['periods'] = ApiClient.convertToType(data['periods'], 'Number');
            }
            if (data.hasOwnProperty('enable_intelligent_aggregation')) {
                obj['enable_intelligent_aggregation'] = ApiClient.convertToType(data['enable_intelligent_aggregation'], 'Boolean');
            }
            if (data.hasOwnProperty('confidence_level')) {
                obj['confidence_level'] = ApiClient.convertToType(data['confidence_level'], 'Number');
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [TrafficForecastingRequestDataInner]);
            }
            if (data.hasOwnProperty('traffic_settings')) {
                obj['traffic_settings'] = TrafficForecastingRequestTrafficSettings.constructFromObject(data['traffic_settings']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrafficForecastingRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrafficForecastingRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TrafficForecastingRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['identifier'] && !(typeof data['identifier'] === 'string' || data['identifier'] instanceof String)) {
            throw new Error("Expected the field `identifier` to be a primitive type in the JSON string but got " + data['identifier']);
        }
        // ensure the json data is a string
        if (data['frequency'] && !(typeof data['frequency'] === 'string' || data['frequency'] instanceof String)) {
            throw new Error("Expected the field `frequency` to be a primitive type in the JSON string but got " + data['frequency']);
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                TrafficForecastingRequestDataInner.validateJSON(item);
            };
        }
        // validate the optional field `traffic_settings`
        if (data['traffic_settings']) { // data not null
          TrafficForecastingRequestTrafficSettings.validateJSON(data['traffic_settings']);
        }

        return true;
    }


}

TrafficForecastingRequest.RequiredProperties = ["identifier", "frequency", "periods", "data", "traffic_settings"];

/**
 * Metric identifier (e.g., endpoint name, service name)
 * @member {String} identifier
 */
TrafficForecastingRequest.prototype['identifier'] = undefined;

/**
 * Data frequency for forecasting: - M: Minute (for real-time traffic) - H: Hourly - D: Daily - W: Weekly - MS: Monthly (start of month) - ME: Monthly (end of month) - Q: Quarterly - Y: Yearly 
 * @member {module:model/TrafficForecastingRequest.FrequencyEnum} frequency
 */
TrafficForecastingRequest.prototype['frequency'] = undefined;

/**
 * Optional start date for the forecast period
 * @member {Date} start_date
 */
TrafficForecastingRequest.prototype['start_date'] = undefined;

/**
 * Number of periods to forecast ahead
 * @member {Number} periods
 */
TrafficForecastingRequest.prototype['periods'] = undefined;

/**
 * Enable intelligent data aggregation for improved forecasting
 * @member {Boolean} enable_intelligent_aggregation
 */
TrafficForecastingRequest.prototype['enable_intelligent_aggregation'] = undefined;

/**
 * Confidence level for forecast intervals (default 0.95)
 * @member {Number} confidence_level
 */
TrafficForecastingRequest.prototype['confidence_level'] = undefined;

/**
 * Historical traffic data for forecasting
 * @member {Array.<module:model/TrafficForecastingRequestDataInner>} data
 */
TrafficForecastingRequest.prototype['data'] = undefined;

/**
 * @member {module:model/TrafficForecastingRequestTrafficSettings} traffic_settings
 */
TrafficForecastingRequest.prototype['traffic_settings'] = undefined;





/**
 * Allowed values for the <code>frequency</code> property.
 * @enum {String}
 * @readonly
 */
TrafficForecastingRequest['FrequencyEnum'] = {

    /**
     * value: "M"
     * @const
     */
    "M": "M",

    /**
     * value: "H"
     * @const
     */
    "H": "H",

    /**
     * value: "D"
     * @const
     */
    "D": "D",

    /**
     * value: "W"
     * @const
     */
    "W": "W",

    /**
     * value: "MS"
     * @const
     */
    "MS": "MS",

    /**
     * value: "ME"
     * @const
     */
    "ME": "ME",

    /**
     * value: "Q"
     * @const
     */
    "Q": "Q",

    /**
     * value: "Y"
     * @const
     */
    "Y": "Y"
};



export default TrafficForecastingRequest;

