/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.1.0
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProductInsightsRequest from './ProductInsightsRequest';

/**
 * The ProductInsightsBatchRequest model module.
 * @module model/ProductInsightsBatchRequest
 * @version 2.1.0
 */
class ProductInsightsBatchRequest {
    /**
     * Constructs a new <code>ProductInsightsBatchRequest</code>.
     * @alias module:model/ProductInsightsBatchRequest
     * @param series {Array.<module:model/ProductInsightsRequest>} One entry per product, each the same shape as a single /v2/product-insights request
     */
    constructor(series) { 
        
        ProductInsightsBatchRequest.initialize(this, series);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, series) { 
        obj['series'] = series;
    }

    /**
     * Constructs a <code>ProductInsightsBatchRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductInsightsBatchRequest} obj Optional instance to populate.
     * @return {module:model/ProductInsightsBatchRequest} The populated <code>ProductInsightsBatchRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductInsightsBatchRequest();

            if (data.hasOwnProperty('series')) {
                obj['series'] = ApiClient.convertToType(data['series'], [ProductInsightsRequest]);
            }
            if (data.hasOwnProperty('batch')) {
                obj['batch'] = ApiClient.convertToType(data['batch'], 'String');
            }
            if (data.hasOwnProperty('parts')) {
                obj['parts'] = ApiClient.convertToType(data['parts'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductInsightsBatchRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductInsightsBatchRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProductInsightsBatchRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['series']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['series'])) {
                throw new Error("Expected the field `series` to be an array in the JSON data but got " + data['series']);
            }
            // validate the optional field `series` (array)
            for (const item of data['series']) {
                ProductInsightsRequest.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['batch'] && !(typeof data['batch'] === 'string' || data['batch'] instanceof String)) {
            throw new Error("Expected the field `batch` to be a primitive type in the JSON string but got " + data['batch']);
        }

        return true;
    }


}

ProductInsightsBatchRequest.RequiredProperties = ["series"];

/**
 * One entry per product, each the same shape as a single /v2/product-insights request
 * @member {Array.<module:model/ProductInsightsRequest>} series
 */
ProductInsightsBatchRequest.prototype['series'] = undefined;

/**
 * Existing batch identifier to append this upload to, for submitting one logical batch in chunks
 * @member {String} batch
 */
ProductInsightsBatchRequest.prototype['batch'] = undefined;

/**
 * Total number of parts the batch will consist of, when submitting in chunks
 * @member {Number} parts
 */
ProductInsightsBatchRequest.prototype['parts'] = undefined;






export default ProductInsightsBatchRequest;

