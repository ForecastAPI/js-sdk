/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.0.2
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TrafficForecastingRequestTrafficSettings model module.
 * @module model/TrafficForecastingRequestTrafficSettings
 * @version 2.0.2
 */
class TrafficForecastingRequestTrafficSettings {
    /**
     * Constructs a new <code>TrafficForecastingRequestTrafficSettings</code>.
     * Infrastructure and traffic configuration
     * @alias module:model/TrafficForecastingRequestTrafficSettings
     * @param currentCapacity {Number} Current infrastructure capacity (requests, users, etc.)
     * @param baselineTraffic {Number} Normal/expected traffic level for comparison
     */
    constructor(currentCapacity, baselineTraffic) { 
        
        TrafficForecastingRequestTrafficSettings.initialize(this, currentCapacity, baselineTraffic);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, currentCapacity, baselineTraffic) { 
        obj['current_capacity'] = currentCapacity;
        obj['baseline_traffic'] = baselineTraffic;
    }

    /**
     * Constructs a <code>TrafficForecastingRequestTrafficSettings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrafficForecastingRequestTrafficSettings} obj Optional instance to populate.
     * @return {module:model/TrafficForecastingRequestTrafficSettings} The populated <code>TrafficForecastingRequestTrafficSettings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrafficForecastingRequestTrafficSettings();

            if (data.hasOwnProperty('current_capacity')) {
                obj['current_capacity'] = ApiClient.convertToType(data['current_capacity'], 'Number');
            }
            if (data.hasOwnProperty('baseline_traffic')) {
                obj['baseline_traffic'] = ApiClient.convertToType(data['baseline_traffic'], 'Number');
            }
            if (data.hasOwnProperty('scaling_buffer')) {
                obj['scaling_buffer'] = ApiClient.convertToType(data['scaling_buffer'], 'Number');
            }
            if (data.hasOwnProperty('scale_up_threshold')) {
                obj['scale_up_threshold'] = ApiClient.convertToType(data['scale_up_threshold'], 'Number');
            }
            if (data.hasOwnProperty('scale_down_threshold')) {
                obj['scale_down_threshold'] = ApiClient.convertToType(data['scale_down_threshold'], 'Number');
            }
            if (data.hasOwnProperty('alert_threshold')) {
                obj['alert_threshold'] = ApiClient.convertToType(data['alert_threshold'], 'Number');
            }
            if (data.hasOwnProperty('anomaly_threshold')) {
                obj['anomaly_threshold'] = ApiClient.convertToType(data['anomaly_threshold'], 'Number');
            }
            if (data.hasOwnProperty('cost_per_unit')) {
                obj['cost_per_unit'] = ApiClient.convertToType(data['cost_per_unit'], 'Number');
            }
            if (data.hasOwnProperty('fixed_cost_per_capacity')) {
                obj['fixed_cost_per_capacity'] = ApiClient.convertToType(data['fixed_cost_per_capacity'], 'Number');
            }
            if (data.hasOwnProperty('base_scaling_time')) {
                obj['base_scaling_time'] = ApiClient.convertToType(data['base_scaling_time'], 'Number');
            }
            if (data.hasOwnProperty('enable_auto_scaling')) {
                obj['enable_auto_scaling'] = ApiClient.convertToType(data['enable_auto_scaling'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrafficForecastingRequestTrafficSettings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrafficForecastingRequestTrafficSettings</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TrafficForecastingRequestTrafficSettings.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

TrafficForecastingRequestTrafficSettings.RequiredProperties = ["current_capacity", "baseline_traffic"];

/**
 * Current infrastructure capacity (requests, users, etc.)
 * @member {Number} current_capacity
 */
TrafficForecastingRequestTrafficSettings.prototype['current_capacity'] = undefined;

/**
 * Normal/expected traffic level for comparison
 * @member {Number} baseline_traffic
 */
TrafficForecastingRequestTrafficSettings.prototype['baseline_traffic'] = undefined;

/**
 * Buffer percentage for scaling decisions (default 0.2 = 20%)
 * @member {Number} scaling_buffer
 */
TrafficForecastingRequestTrafficSettings.prototype['scaling_buffer'] = undefined;

/**
 * Utilization threshold to trigger scale up (default 0.8 = 80%)
 * @member {Number} scale_up_threshold
 */
TrafficForecastingRequestTrafficSettings.prototype['scale_up_threshold'] = undefined;

/**
 * Utilization threshold to trigger scale down (default 0.3 = 30%)
 * @member {Number} scale_down_threshold
 */
TrafficForecastingRequestTrafficSettings.prototype['scale_down_threshold'] = undefined;

/**
 * Traffic spike alert threshold as multiplier of baseline (default 1.5 = 150%)
 * @member {Number} alert_threshold
 */
TrafficForecastingRequestTrafficSettings.prototype['alert_threshold'] = undefined;

/**
 * Standard deviations for anomaly detection (default 3.0)
 * @member {Number} anomaly_threshold
 */
TrafficForecastingRequestTrafficSettings.prototype['anomaly_threshold'] = undefined;

/**
 * Variable cost per traffic unit (default 0.01)
 * @member {Number} cost_per_unit
 */
TrafficForecastingRequestTrafficSettings.prototype['cost_per_unit'] = undefined;

/**
 * Fixed cost per capacity unit (default 0.1)
 * @member {Number} fixed_cost_per_capacity
 */
TrafficForecastingRequestTrafficSettings.prototype['fixed_cost_per_capacity'] = undefined;

/**
 * Base time in minutes for scaling operations (default 5)
 * @member {Number} base_scaling_time
 */
TrafficForecastingRequestTrafficSettings.prototype['base_scaling_time'] = undefined;

/**
 * Whether auto-scaling is currently enabled (default false)
 * @member {Boolean} enable_auto_scaling
 */
TrafficForecastingRequestTrafficSettings.prototype['enable_auto_scaling'] = undefined;






export default TrafficForecastingRequestTrafficSettings;

