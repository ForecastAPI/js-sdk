/**
 * ForecastAPI
 * Time series forecasting service with multiple algorithms and automatic method selection
 *
 * The version of the OpenAPI document: 2.2.0
 * Contact: support@forecastapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AdjustedForecastAdjustmentsAppliedInner from './AdjustedForecastAdjustmentsAppliedInner';
import ForecastPeriod from './ForecastPeriod';

/**
 * The AdjustedForecast model module.
 * @module model/AdjustedForecast
 * @version 2.2.0
 */
class AdjustedForecast {
    /**
     * Constructs a new <code>AdjustedForecast</code>.
     * The what-if scenario path. Present only when &#x60;adjustments&#x60; was sent, as a sibling of &#x60;result&#x60; — never a field inside it. The adjusted path is never stored, so scenarios cannot pollute accuracy tracking. 
     * @alias module:model/AdjustedForecast
     */
    constructor() { 
        
        AdjustedForecast.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AdjustedForecast</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AdjustedForecast} obj Optional instance to populate.
     * @return {module:model/AdjustedForecast} The populated <code>AdjustedForecast</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AdjustedForecast();

            if (data.hasOwnProperty('forecasts')) {
                obj['forecasts'] = ApiClient.convertToType(data['forecasts'], [ForecastPeriod]);
            }
            if (data.hasOwnProperty('adjustments_applied')) {
                obj['adjustments_applied'] = ApiClient.convertToType(data['adjustments_applied'], [AdjustedForecastAdjustmentsAppliedInner]);
            }
            if (data.hasOwnProperty('stored')) {
                obj['stored'] = ApiClient.convertToType(data['stored'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AdjustedForecast</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AdjustedForecast</code>.
     */
    static validateJSON(data) {
        if (data['forecasts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['forecasts'])) {
                throw new Error("Expected the field `forecasts` to be an array in the JSON data but got " + data['forecasts']);
            }
            // validate the optional field `forecasts` (array)
            for (const item of data['forecasts']) {
                ForecastPeriod.validateJSON(item);
            };
        }
        if (data['adjustments_applied']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['adjustments_applied'])) {
                throw new Error("Expected the field `adjustments_applied` to be an array in the JSON data but got " + data['adjustments_applied']);
            }
            // validate the optional field `adjustments_applied` (array)
            for (const item of data['adjustments_applied']) {
                AdjustedForecastAdjustmentsAppliedInner.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The baseline path with every adjustment applied, in array order
 * @member {Array.<module:model/ForecastPeriod>} forecasts
 */
AdjustedForecast.prototype['forecasts'] = undefined;

/**
 * Echo of each adjustment as applied, in order
 * @member {Array.<module:model/AdjustedForecastAdjustmentsAppliedInner>} adjustments_applied
 */
AdjustedForecast.prototype['adjustments_applied'] = undefined;

/**
 * Always false — only the baseline in `result` is persisted and scored for accuracy
 * @member {Boolean} stored
 */
AdjustedForecast.prototype['stored'] = undefined;






export default AdjustedForecast;

